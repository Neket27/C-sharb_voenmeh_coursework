<Window x:Class="C_sharb_voenmeh_coursework.MainWindow"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:app"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        Title="Проводник" Height="450" Width="800"
        x:Name="Window">
    <Grid Background="#ffdadada">

        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>


        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <Button Width="50" Height="30" FontSize="15px" Content="&lt;-" Command="{Binding MoveBackCommand}"></Button>
            <Button Width="50" Height="30" FontSize="15px" Content="-&gt;" Command="{Binding MoveForwardCommand}"></Button>
            <Button Width="50" Height="30" FontSize="15px" Content="O"></Button>
        </StackPanel>

        <ListBox Background="#ebd7f7" 
                 Grid.Row="1" Grid.Column="0" 
                 Grid.RowSpan="2" 
                 ItemsSource="{Binding DirectoriesAndFilesLeftPanel}"
                 SelectedItem="{Binding SelectedFileEntity}">
            
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDoubleClick">
                <b:InvokeCommandAction Command="{Binding OpenCommand}"
                                CommandParameter="{Binding SelectedFileEntity}"></b:InvokeCommandAction>
                </b:EventTrigger>
            </b:Interaction.Triggers>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock FontSize="14"  Text="{Binding Name}"></TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

        <TextBox Grid.Row="0" 
                 Grid.Column="1" 
                 Background="Bisque" 
                 Text="{Binding FilePath}" 
                 x:Name="PathFile" 
                  >

            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDoubleClick">
                    <b:InvokeCommandAction Command="{Binding DataContext.OpenCommand,ElementName=Window}"
                                CommandParameter="{Binding Text,ElementName=PathFile}"></b:InvokeCommandAction>
                </b:EventTrigger>
            </b:Interaction.Triggers>
            
        </TextBox>



        <!--<ListView Grid.Column="1" Grid.Row="1" x:Name="ListDisksAndDirsAndFiles">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock  Text="{Binding DirectoryName}"></TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->


        <ListBox Grid.Column="1" 
                 Grid.Row="1"
                 Grid.RowSpan="2"
                 ItemsSource="{Binding DirectoriesAndFiles}"
                 SelectedItem="{Binding SelectedFileEntity}">

            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDoubleClick">
                    <b:InvokeCommandAction Command="{Binding OpenCommand}"
                                CommandParameter="{Binding SelectedFileEntity}"></b:InvokeCommandAction>
                </b:EventTrigger>
            </b:Interaction.Triggers>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding DirectoryName}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Column="1" Grid.Row="2" x:Name="label2"></TextBlock>


    </Grid>
</Window>
